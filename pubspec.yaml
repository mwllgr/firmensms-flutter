name: firmensms
description: A mobile client for the firmensms.at REST API
publish_to: 'none'
version: 1.3.1+15

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  material_ui: ^1.2.0
  cupertino_icons: ^1.0.9
  flutter_form_builder: ^11.0.0
  form_builder_validators: ^11.3.0
  flutter_native_contact_picker: ^0.0.12
  flutter_secure_storage: ^11.1.1
  http: ^1.6.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  shared_preferences_platform_interface: ^2.4.2

flutter:
  uses-material-design: true
